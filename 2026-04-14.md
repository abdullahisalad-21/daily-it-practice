# 2026‑4‑14 — Daily Practice: OpenSSL Key Pairs, Encryption, Signing, and Hashing

## ⭐ 1. Overview  
Today’s session focused on two core security areas: asymmetric cryptography with OpenSSL and integrity verification using hashing algorithms. I practiced generating RSA key pairs, encrypting and decrypting data, creating and verifying digital signatures, and using MD5, SHA‑1, and SHA‑256 to detect file tampering. This work strengthened my understanding of confidentiality, integrity, authenticity, and non‑repudiation, and gave me practical experience with tools that are essential in real IT and cybersecurity environments.

---

## 🎯 2. Objectives  
- Generate RSA private and public keys  
- Encrypt and decrypt data using RSA  
- Create and verify digital signatures  
- Understand how asymmetric cryptography supports secure communication  
- Create text files for hashing  
- Generate and verify MD5, SHA‑1, and SHA‑256 hashes  
- Demonstrate hash verification failure after modifying files  
- Document both labs using my 8‑section structure  
- Write reflection reports for both labs  

---

## 🛠️ 3. Tools & Commands Used  

### OpenSSL (Lab 01)  
- `openssl genrsa -out private_key.pem 2048`  
- `openssl rsa -in private_key.pem -pubout -out public_key.pem`  
- `openssl rsautl -encrypt -pubin -inkey public_key.pem -in secret.txt -out secret.enc`  
- `openssl rsautl -decrypt -inkey private_key.pem -in secret.enc`  
- `openssl dgst -sha256 -sign private_key.pem -out secret.txt.sha256 secret.txt`  
- `openssl dgst -sha256 -verify public_key.pem -signature secret.txt.sha256 secret.txt`  

### Hashing (Lab 02)  
- `echo "text" > file.txt`  
- `md5sum file.txt > file.txt.md5`  
- `md5sum -c file.txt.md5`  
- `cp file.txt badfile.txt`  
- `md5sum badfile.txt > badfile.txt.md5`  
- `nano badfile.txt`  
- `md5sum -c badfile.txt.md5`  
- `md5sum badfile.txt > new.badfile.txt.md5`  
- `shasum file.txt > file.txt.sha1`  
- `shasum -c file.txt.sha1`  
- `shasum -a 256 file.txt > file.txt.sha256`  
- `shasum -a 256 -c file.txt.sha256`  

### Documentation Work  
- Full 8‑section Markdown documentation for Lab 01  
- Full 8‑section Markdown documentation for Lab 02  
- Reflection reports for both labs  

---

## 🧩 4. Steps Completed  

### 4.1 I Completed Lab 01 (OpenSSL)  
I generated a 2048‑bit RSA private key and extracted the public key. I encrypted a message using the public key and decrypted it using the private key. I then created a SHA‑256 digital signature and verified it successfully. I documented the entire lab and wrote a reflection report.

### 4.2 I Started Lab 02 (Hashing)  
I created a text file and generated its MD5 hash. I verified the hash successfully.

### 4.3 I Demonstrated MD5 Verification Failure  
I copied the file, generated a matching hash, then modified the file by adding a single space. Verification failed, proving how sensitive hashing is.

### 4.4 I Recomputed the MD5 Hash  
I generated a new MD5 hash for the modified file and verified it successfully.

### 4.5 I Generated and Verified SHA‑1 and SHA‑256 Hashes  
I created SHA‑1 and SHA‑256 digests and verified both successfully. The SHA‑256 hash was significantly longer, demonstrating its stronger security.

### 4.6 I Completed Documentation and Reflection  
I wrote full 8‑section documentation for Lab 02 and a reflection report matching the style of Lab 01. I then prepared this Daily Practice entry summarizing the entire session.

---

## 🐞 5. Problems & Fixes  
I didn’t encounter any issues during this session. All commands worked as expected, and the behavior matched the lab instructions.

---

## 📚 6. What I Learned  
- How RSA key pairs support encryption, decryption, and digital signatures  
- How hashing ensures integrity and detects tampering  
- Why MD5 is weak, SHA‑1 is outdated, and SHA‑256 is the modern standard  
- How to verify file integrity using `md5sum` and `shasum`  
- How even a single character change completely alters a hash  
- How asymmetric cryptography supports confidentiality, authenticity, and non‑repudiation  
- How to document labs professionally using my 8‑section structure  

---

## 📎 7. Related Files  
- `private_key.pem`  
- `public_key.pem`  
- `secret.txt`  
- `secret.enc`  
- `secret.txt.sha256`  
- `file.txt`  
- `file.txt.md5`  
- `badfile.txt`  
- `badfile.txt.md5`  
- `new.badfile.txt.md5`  
- `file.txt.sha1`  
- `file.txt.sha256`  

---

## 🔜 8. Next Steps  
- Continue to the next lab in the IT Security Basics course  
- Compare hashing with encryption and digital signatures  
- Practice verifying downloaded software using SHA‑256 checksums  
- Push this documentation to my Daily Practice repo  
